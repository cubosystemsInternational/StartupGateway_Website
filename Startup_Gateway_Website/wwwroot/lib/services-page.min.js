$(function () {
    'use strict';

    init();

    function init() {
        var activeSection = $('#active-section').val();

        $('a[data-target$="' + activeSection + '"]').parent().addClass('active');
        $('#' + activeSection).addClass('active');
        $('#' + activeSection).addClass('in');
    }
});